

export interface ReferentielBaseLookupDto extends CodableReferenceDataLookupDto<number,string> {  
}

export enum AnneeAcademiqueEtat { 
 	Draft = 0,
 	NotStarted = 1,
 	InProgress = 2,
 	Completed = 3, 
}

